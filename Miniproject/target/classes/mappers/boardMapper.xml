<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.miniproj.mappers.boardMapper">
	<select id="getAllBoard" resultType="com.miniproj.domain.Board">
		select * from board order by no desc
	</select>

	<insert id="insertNewBoard">
		insert into board(writer, title, content) 
		values(#{writer}, #{title}, #{content})
	</insert>
	
	<select id="getNo" resultType="int">
		select max(no) from board
	</select>
	
	<insert id="insertUploadedFile">
		insert into uploadedfile(originFileName, newFileName, size, boardNo, thumbFileName)
		values(#{originalFileName}, #{newFileName}, #{fileSize}, #{boardNo}, #{thumbFileName})
	</insert>
</mapper>